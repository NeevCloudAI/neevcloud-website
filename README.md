1. Tooltip missing for Validate Model icon.
2. Recommendation tooltip is also missing
3. Can we just use Nvidia 5090 instead of full GPU name, because the main GPU identify is hidden. If not can we display the full GPU name in next line?
4. Don't use _ here, use x (2_ NVIDIA GeForce RTX 5090)
5. The price range doesn't look good for filters, use proper range like 50, 100, 300, 500, 800, 1000 etc
6. The message should be at the center of the grid, with centered aligned if filter does not match any criteria.
7. The side drawer is covering more than 60-65% of the screen. It should only cover 50% of the screen. This doesn't look good
8. The green tick mark for Recommended GPU is missing from GPU dropdown.
9. 2 deployments are in progress but we are still showing Deploy your first model at the top. We should remove that.
10. Can we add a tooltip message to explain the reason for failure?
11. P0 Activity logs showing all 3 models deployed.
12. Same for GPU Count, the recommended GPU count should have a green tick to indicate which count is recommended.

•·Neevcloud website

sliders images
header

SEO: add seo object in every page
