export type ExploreModelsStep = {
  badgeLabel: string;
  title: string;
  description: string;
  features: readonly string[];
};

export type ExploreModelsStepPreviewProps = {
  stepIndex: number;
};
