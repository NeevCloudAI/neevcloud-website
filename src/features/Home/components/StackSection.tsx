import StackScrollClient from "./StackScrollClient";

export default function StackSection() {
  return (
    <section aria-labelledby="stack-heading" className="bg-white">
      <StackScrollClient />
    </section>
  );
}
