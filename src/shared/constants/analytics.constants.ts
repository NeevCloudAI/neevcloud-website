const isProduction = process.env.NODE_ENV === "production";

export const GTM_ID = isProduction ? "GTM-PTHFJ68" : "";

export const GA_ID = isProduction ? "G-S0VEP2ZKBP" : "";

export const CLARITY_ID = isProduction ? "k6pci2nqx0" : "";
