import StackScrollClient from "./StackScrollClient";

export default function StackSection() {
  return (
    <section aria-labelledby="stack-heading" className="bg-cloud-gray">
      <StackScrollClient />
    </section>
  );
}
