import CPUConfigurationPanelClient from "@/features/cpu-cluster/components/CPUConfigurationPanelClient";

const CPUConfigurationPanel = () => {
  return <CPUConfigurationPanelClient />;
};

export default CPUConfigurationPanel;
